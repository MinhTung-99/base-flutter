enum POPUP_MENU_VALUE {
  FIRST_ITEM('FIRST_ITEM'),
  SECONDE_ITEM('SECONDE_ITEM');

  const POPUP_MENU_VALUE(this.value);

  final String value;
}

enum FONTS {
  DEFAULT('FIRST_ITEM');

  const FONTS(this.value);

  final String value;
}